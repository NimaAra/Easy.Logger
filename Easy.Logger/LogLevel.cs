namespace Easy.Logger
{
    /// <summary>
    /// Determines the level as which a log entry should be logged.
    /// </summary>
    public enum LogLevel
    {
        Debug = 0,
        Info,
        Warn,
        Error,
        Fatal
    }
}